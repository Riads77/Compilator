/* test-table-symboles.tpc */

/* Test file for simplified translator of a declaration of variables in C */

int r1,b,s,c,r2 ;
int test;
int test;
char letter, digit, punct;
int x;
struct Point {
    int x;
    int y;
};

struct Point point;

int f (int x, char y) {
    if (y == 'a') return x;
    return x + 1;
}

void vide (void){
    return;
}

struct Point fp(int x, int y, struct Point p) {
    struct Point res;
    struct StructInutile {
        char c;
    };
    res.x = x + p.y;
    res.y = y + p.y;
    return res;
}

int main(void) {
   int y;
   x = f(5, 'a');
   r1=12;
   r2=24;
   b=r1+r2;
   letter = 'i';
   punct = '.';
   guzedoauzd = 7784==7845;
   if(b == 5) b = b+1;
   if (b ==6) {
       b = 5;
       r2 = r1;
   }
   else {
       r1 = r2;
   }

    while (b > 0) {
        b = b - 1;
    }

    reade(x);
    readc(i);
    print(57);



   return b;
}
